# =============================================================================
# Pantolingo Environment Variables
# =============================================================================
# Copy this file to .env and fill in your values.
# The .env file should be at the monorepo root - both apps load from there.
#
# NEVER commit .env to version control!
# =============================================================================

# -----------------------------------------------------------------------------
# DATABASE (Required - used by both apps)
# -----------------------------------------------------------------------------
# PostgreSQL connection string for the shared database.
# Used by: translate app, www app (via @pantolingo/db package)
#
# Format: postgresql://[user]:[password]@[host]:[port]/[database]?sslmode=require
# Example (Render): postgresql://user:pass@oregon-postgres.render.com/pantolingo
# Example (local):  postgresql://localhost:5432/pantolingo
POSTGRES_DB_URL=

# -----------------------------------------------------------------------------
# TRANSLATION API (Required for translate app)
# -----------------------------------------------------------------------------
# OpenRouter API key for AI-powered translations.
# Used by: translate app only
#
# Get your key at: https://openrouter.ai/keys
# The translate app uses this to call LLMs (Claude, GPT, etc.) for translations.
OPENROUTER_API_KEY=

# -----------------------------------------------------------------------------
# OPTIONAL SETTINGS
# -----------------------------------------------------------------------------

# Server port for the translate app (default: 8787)
# Used by: translate app only
# PORT=8787

# Maintenance mode message for the translate app (empty = normal operation)
# Used by: translate app only
# When set, all requests return a 503 maintenance page with this message.
# MAINTENANCE_MESSAGE=We're updating our systems. Please check back shortly.

# Google Cloud Project ID (currently unused, reserved for future use)
# Used by: translate app only
# GOOGLE_PROJECT_ID=

# -----------------------------------------------------------------------------
# AUTHENTICATION (Required for www app)
# -----------------------------------------------------------------------------
# Auth.js v5 configuration for magic link authentication.
# Used by: www app only
#
# Generate AUTH_SECRET: openssl rand -base64 32
AUTH_SECRET=

# -----------------------------------------------------------------------------
# EMAIL / SMTP (Required for @pantolingo/smtp package)
# -----------------------------------------------------------------------------
# SMTP credentials for sending emails.
# Used by: @pantolingo/smtp package (available to both apps)
#
# Works with any SMTP provider (AWS SES, SendGrid, Mailgun, Gmail, etc.)
SMTP_HOST=
SMTP_USER=
SMTP_PASSWORD=
SMTP_FROM=
# SMTP_PORT=587
# SMTP_SECURE=false

# -----------------------------------------------------------------------------
# CLOUDFLARE TURNSTILE (Required for www app)
# -----------------------------------------------------------------------------
# Turnstile CAPTCHA for bot protection on auth forms.
# Used by: www app only
#
# Get your keys at: https://dash.cloudflare.com/?to=/:account/turnstile
#
# Test keys for development (always pass):
#   Site key:   1x00000000000000000000AA
#   Secret key: 1x0000000000000000000000000000000AA
TURNSTILE_SITE_KEY=
TURNSTILE_SECRET_KEY=
